import React from "react";

export const ArrowMoveUpRight = ({ className }) => {
  return (
    <img
      className={`absolute w-5 h-5 top-0 left-0 ${className}`}
      alt="Arrow move up right"
      src="arrow-move-up-right.svg"
    />
  );
};